fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
httpx==0.27.0

# Uncomment for Phase 3 — real ML models
# sentence-transformers==3.0.0
# Pillow==10.3.0
# torch==2.3.0
# transformers==4.41.0

# ── Image verification pipeline (app/image_analysis.py) ────────
Pillow==10.3.0
opencv-python-headless==4.10.0.84
numpy==1.26.4
imagehash==4.3.1
python-multipart==0.0.9
psycopg2-binary==2.9.9

# Only required when ML_MODE=trained (see app/services/classifier.py).
# In ML_MODE=demo (default) these are not imported and don't need to
# be installed, which keeps local/demo setup lightweight.
torch==2.3.0
torchvision==0.18.0
